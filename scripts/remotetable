#!/usr/bin/env bash
# Host CLI wrapper: scripts/remotetable …
set -euo pipefail
ROOT=$(cd "$(dirname "$0")/.." && pwd)
export PYTHONPATH="$ROOT/python${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m remotetable "$@"
