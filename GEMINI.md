# GEMINI.md — library host overlay

Use with AGENTS.md / AGENT_MANDATES.md / GROK.md.
This repo uses generic `./run-grok-*` launchers and `.grok/prompts/packs/`.
Sandbox: `./sandbox/` (plans under `sandbox/plans/`).
project.config is local-only (gitignored); see project.config.example.
