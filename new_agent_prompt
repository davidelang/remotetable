# Mandate Acknowledgment (Fast-Track)

You are in a **Multi-Agent Orchestration** environment.

**MANDATORY STARTUP:**

1. Read `./AGENT_CONTEXT.md` (or template + `git status` if missing).
2. Read CLI overlay `./GROK.md` or `./GEMINI.md`, and `./AGENT_MANDATES.md`.
3. Read full `./project-facts.md`.
4. Confirm `pwd` once. Never prefix helpers with `cd … &&`.
5. Read the rest of your **role pack** from `AGENTS.md` (planner/coder/master extras).

Do **not** re-read the full pack every turn. After `/compact`, re-read the pack.

**Brief report:**

1. **Overrides:** Sandbox write exemption; testing exemption (forensic + build).
2. **Barrier:** Planning = no app source edits; execution only after magic approval of a named sandbox plan.
3. **Verification:** Forensic read after edits; `./build_app`; coder **plan completeness** before handoff.
4. **Geography:** Role + Branch; never `..` in paths; sandbox `/home/dlang/git/VehicleExpenses-automated/dev-ai-interaction/`.
5. **Special files:** TODO = future only (`todo-append`/`todo-close`); eng-log via `./append-to-engineering-log` only.
6. **Handoff:** After ready-to-test + END marker, turn is over; feedback = new planning cycle.
7. **Native plan mode:** Do **not** enter it if you are **planner** or **coder**. Optional only for bare `run-grok` / orchestrator. STOP after this report and wait for a user directive (planner may stay interactive for planning only).

**Research:** Prefer **chat** for findings; files only if asked or durable cache — still discuss issues in chat.

*User tip:* Multiline input — **Ctrl+M** or **/multiline** (**/ml**).

**Cwd fixed** after geography. Invoke `./append-to-engineering-log` etc. without `cd … &&`.
